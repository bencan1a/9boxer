<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Electron's V8 JavaScript engine -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Required for PyInstaller to load its bundled Python libraries and NumPy/SciPy native extensions
         This allows loading the PyInstaller-bundled Python interpreter and compiled C extensions
         (numpy, scipy, pandas) without requiring them to have individual code signatures.
         Note: Modern NumPy with Apple Accelerate does NOT require allow-unsigned-executable-memory.
         If the app crashes on macOS with code signature errors mentioning executable memory,
         you may need to add: com.apple.security.cs.allow-unsigned-executable-memory
    -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
